{
  "compilerOptions": {
    "module": "ESNext",               // Usar módulos ESNext
    "target": "ESNext",               // Usar la versión más moderna de ECMAScript
    "outDir": "./dist",               // Carpeta de salida para los archivos compilados
    "rootDir": "./app",               // Directorio de entrada (donde están tus archivos .ts)
    "moduleResolution": "node",       // Resolver módulos usando la resolución de módulos Node.js
    "esModuleInterop": true,          // Compatibilidad con módulos CommonJS y ES6
    "strict": true,                   // Habilitar comprobaciones estrictas
    "resolveJsonModule": true,        // Permitir importar archivos .json
    "skipLibCheck": true,             // Saltarse la verificación de tipos de las bibliotecas
    "allowImportingTsExtensions": true,  // Permitir importar extensiones .ts
    "noEmit": true                 // Cambiar a false para permitir la compilación y emisión de archivos .js
  },
  "include": ["app/**/*"],            // Incluir todos los archivos en la carpeta 'app'
  "exclude": ["node_modules"]         // Excluir la carpeta 'node_modules'
}
